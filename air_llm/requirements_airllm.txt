# AirLLM Kaggle Pipeline - Dependencies
# Install: pip install -r requirements_airllm.txt

# Core - AirLLM (layer-by-layer large model inference)
airllm>=2.10.0

# Dataset & DataFrame
datasets>=2.19.0
pandas>=2.0.0

# Tokenizer & Model utilities
transformers>=4.40.0
huggingface-hub>=0.23.0
accelerate>=0.30.0

# HTML Pruning
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Token counting
tiktoken>=0.7.0

# GPU support (auto-installed on Kaggle)
# torch>=2.0.0
